## SDXL setup for X-FLASH

1. Install AUTOMATIC1111 WebUI.
2. Put `sd_xl_base_1.0.safetensors` into:
   `webui\models\Stable-diffusion\`
3. Start WebUI with API enabled:
   `--api`
4. In X-FLASH set SD API URL to:
   `http://127.0.0.1:7860`
